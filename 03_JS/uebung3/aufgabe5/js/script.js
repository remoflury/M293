/**
 * AUFGABE 5
 *
 * Verwenden Sie fetch(), um von users.json die User-Daten zu laden und wandeln Sie
 *  die Daten in JSON um.
 *
 * Geben Sie nach erfolgreichem Laden den Namen des ersten Nutzers
 * mit console.log() in der Konsole aus.
 *
 * Tipp: der Pfad der Datei ist relativ zu dort wo die Funktion ausgeführt wird (index.html)
 */
