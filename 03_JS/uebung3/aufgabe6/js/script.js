/**
 * AUFGABE 6
 * 
 * Verwende fetch(), um von data.json die User-Daten zu laden und wandle
 * die Daten in JSON um.
 * 
 * Berechnne das Durchschnittsalter und gebe das Resultat
 * im HTML aus.
 */