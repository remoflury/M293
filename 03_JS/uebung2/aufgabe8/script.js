/**
 * AUFGABE 8
 *
 * Erstellen Sie eine Funktion mit dem Namen trafficLightAction,
 * die eine Farbe als Parameter entgegennimmt (Text, z.B. "rot").
 *
 * Nutzen Sie ein switch-Statement, um je nach Farbe eine Anweisung
 * in der Konsole auszugeben:
 * - "rot":   "Stehen bleiben"
 * - "gelb":  "Achtung"
 * - "gruen": "Fahren"
 *
 * Für alle anderen Werte soll im default-Fall
 * "Ungültige Farbe" ausgegeben werden.
 *
 * Testen Sie die Funktion mit "rot", "gelb", "gruen"
 * und mindestens einem ungültigen Wert (z.B. "blau").
 * 
 */
