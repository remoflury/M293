/**
 * AUFGABE 9
 *
 * Erstellen Sie eine Funktion mit dem Namen showEvenNumbers,
 * die eine Zahl max als Parameter entgegennimmt.
 *
 * Nutzen Sie eine for-Schleife, um von 1 bis max zu zählen.
 * Innerhalb der Schleife prüfen Sie mit if, ob die aktuelle Zahl
 * gerade ist. Nur gerade Zahlen sollen in der Konsole ausgegeben werden.
 *
 * Beispiel: showEvenNumbers(8) gibt 2, 4, 6, 8 aus.
 *
 * Testen Sie die Funktion mit mindestens zwei verschiedenen Werten.
 */
