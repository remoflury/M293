/**
 * AUFGABE 10
 *
 * Wenn der Button "Berechnen" geklickt wird, sollen die beiden Zahlen
 * und die gewählte Rechenart ausgelesen werden.
 *
 * Erstellen Sie eine wiederverwendbare Funktion berechne,
 * die drei Parameter entgegennimmt: zahl1, zahl2 und operator.
 *
 * Nutzen Sie ein switch-Statement, um je nach operator
 * das Ergebnis zu berechnen und in der Konsole auszugeben:
 * - "+": Addition
 * - "-": Subtraktion
 * - "*": Multiplikation
 * - "/": Division
 *
 * Bonus:
 * - Wenn durch 0 geteilt wird, soll mit window.alert
 *   auf den Fehler hingewiesen werden (kein console.log des Ergebnisses).
 * - Wenn ein Input-Feld leer ist oder keine Zahl enthält,
 *   soll ebenfalls ein Alert erscheinen.
 */
