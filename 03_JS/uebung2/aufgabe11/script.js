/**
 * AUFGABE 11
 *
 * Erstellen Sie eine Funktion mit dem Namen analyzeNumbers,
 * die ein Array von Zahlen als Parameter entgegennimmt.
 *
 * Die Funktion soll mit einer for-Schleife durch das Array iterieren
 * und mit if / else if / else zählen, wie viele Zahlen:
 * - positiv sind (grösser als 0)
 * - negativ sind (kleiner als 0)
 * - genau 0 sind
 *
 * Am Ende sollen die drei Zähler in der Konsole ausgegeben werden,
 * z.B.: "Positiv: 4, Negativ: 2, Null: 1"
 *
 * Rufen Sie die Funktion mit dem vorgegebenen Array zahlen auf.
 */

const numbers = [12, -4, 0, 7, 22, -1, 8, 0, -15];
